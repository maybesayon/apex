# Dependencies for the FastAPI service only.
#
# Deliberately excludes streamlit, plotly and schedule: the API never
# imports them, and installing them roughly doubles build time on a free
# tier. The Streamlit app still uses requirements.txt.

# Web
fastapi==0.141.1
uvicorn[standard]==0.53.0

# Identity and storage
pyjwt==2.15.0
sqlalchemy==2.1.0
alembic==1.20.0
psycopg[binary]==3.2.3      # Postgres driver; unused when DATABASE_URL is sqlite

# Analysis engine
pandas==3.0.1
numpy==2.4.3
scikit-learn==1.8.0
ta==0.11.0

# Market data
yfinance==1.2.0
finnhub-python==2.4.27
tradingview-ta==3.3.0
requests==2.33.0
lxml==6.1.1

# Integrations
anthropic==0.89.0
python-dotenv==1.2.2
